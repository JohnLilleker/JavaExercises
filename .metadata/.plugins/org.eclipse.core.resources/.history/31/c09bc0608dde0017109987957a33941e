
public class Bike extends Vehicle {

	private boolean hasEngine;

	public Bike(boolean hasEngine, int wheels, int length, int weight, String id) {
		super(id, wheels, length, weight);
		this.hasEngine = hasEngine;
	}

	public boolean isEnginePowered() {
		return hasEngine;
	}
}
