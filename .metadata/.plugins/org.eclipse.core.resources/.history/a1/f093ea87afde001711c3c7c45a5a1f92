package paintExercise;

public class PaintWizard {

	private Paint p1 = new Paint("CheapoMax", 20, 19.99f, 10);
	private Paint p2 = new Paint("AverageJoes", 15, 17.99f, 11);
	private Paint p3 = new Paint("DuluxourousPaints", 10, 25.f, 20);

	public PaintWizard() {
		// initialise paint here?
	}

	public Paint bestForFloor(int floorSize) {
		// TODO this logic

		// what do I want to know
		// pure coverage can be calculated by capacity * coverage
		int coverage1 = p1.getCapacity() * p1.getCoverage();
		int coverage2 = p2.getCapacity() * p2.getCoverage();
		int coverage3 = p3.getCapacity() * p3.getCoverage();

		// idk maths...

		// no idea...

		return null;
	}
}
