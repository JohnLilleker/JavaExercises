import java.util.ArrayList;

public class Garage {

	private ArrayList<Vehicle> vehicles;

	public Garage(Vehicle... initialCars) {
		vehicles = new ArrayList<>();
		for (Vehicle car : initialCars) {
			vehicles.add(car);
		}
	}

	public void calculateBill() {
		for (Vehicle vehicle : vehicles) {
			System.out.println("Calculating bill on " + vehicle.getID());
			if (vehicle instanceof Car) {
				System.out.println("Bill is on Car: " + vehicle.getWeight() * 100);
			}
			if (vehicle instanceof Bike) {
				System.out.println("Bill is on Car: " + vehicle.getWeight() * 25);
			}
			if (vehicle instanceof Tank) {
				System.out.println("Bill is on Car: " + vehicle.getWeight() * 7500);
			}
		}
	}

	public void printVehicles() {
		vehicles.forEach(vehicle -> System.out.println(vehicle));
	}

}
